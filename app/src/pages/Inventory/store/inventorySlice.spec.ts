import { assert } from 'console';
import inventoryPageReducer, { InventoryPageState } from './inventorySlice';

describe('inventory page reducer', () => {
    const initialState: InventoryPageState = {
    };

    it('should be good', () => {
        assert(true)
    });

});