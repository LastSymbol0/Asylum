import { assert } from 'console';
import libraryPageReducer, { LibraryPageState } from './librarySlice';

describe('library page reducer', () => {
    const initialState: LibraryPageState = {
    };

    it('should be good', () => {
        assert(true)
    });

});