import { assert } from 'console';
import gamesStorePageReducer, { GamesStorePageState } from './gamesStoreSlice';

describe('gamesStore page reducer', () => {
    const initialState: GamesStorePageState = {
    };

    it('should be good', () => {
        assert(true)
    });

});