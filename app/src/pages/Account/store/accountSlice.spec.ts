import { assert } from 'console';
import accountPageReducer, { AccountPageState } from './accountSlice';

describe('account page reducer', () => {
    const initialState: AccountPageState = {
    };

    it('should be good', () => {
        assert(true)
    });

});