import { assert } from 'console';
import marketplacePageReducer, { MarketplacePageState } from './marketplaceSlice';

describe('marketplace page reducer', () => {
    const initialState: MarketplacePageState = {
    };

    it('should be good', () => {
        assert(true)
    });

});