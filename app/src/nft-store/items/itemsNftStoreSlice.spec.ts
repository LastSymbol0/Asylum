import { assert } from 'console';
import itemsNftStoreReducer, { ItemsNftStoreState } from './itemsNftStoreSlice';

describe('itemsNftStore reducer', () => {
    const initialState: ItemsNftStoreState = {
    };

    it('should be good', () => {
        assert(true)
    });

});