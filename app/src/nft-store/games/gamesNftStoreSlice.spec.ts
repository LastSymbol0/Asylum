import { assert } from 'console';
import gamesNftStoreReducer, { GamesNftStoreState } from './gamesNftStore';

describe('gamesNftStore reducer', () => {
    const initialState: GamesNftStoreState = {
    };

    it('should be good', () => {
        assert(true)
    });

});